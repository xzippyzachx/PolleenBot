import UsersMe from '../interfaces/types/UsersMeTypes';
import Transport from '../base/Transport';
declare class UsersClient {
    private transport;
    constructor(transport: Transport);
    /**
     * Return user information about an authorized user. User rate limit for OAuth 2.0 and OAuth 1.0a: 75 requests per 15-minute window per each authenticated user.
     *
     * @link https://developer.twitter.com/en/docs/twitter-api/users/lookup/api-reference/get-users-me
     */
    usersMe(): Promise<UsersMe>;
}
export default UsersClient;
